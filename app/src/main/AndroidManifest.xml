<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.matinus.footballprediction">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconfinder_fieldfootball_1291751"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconfinder_fieldfootball_1291751"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".homeScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".analysisDefinition"
            android:parentActivityName="c.matinus.footballprediction.homeScreen"
            android:screenOrientation="portrait"
            android:label="@string/teamChoosingScreenTitle"></activity>
    </application>

</manifest>